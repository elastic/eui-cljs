(ns eui.services.is-within-hooks
  (:require ["@elastic/eui/lib/services/breakpoint/is_within_hooks.js" :as eui]))

(def useIsWithinMaxBreakpoint eui/useIsWithinMaxBreakpoint)

(def useIsWithinBreakpoints eui/useIsWithinBreakpoints)

(def useIsWithinMinBreakpoint eui/useIsWithinMinBreakpoint)
