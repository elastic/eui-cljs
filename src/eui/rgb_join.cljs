(ns eui.rgb-join
  (:require ["@elastic/eui/lib/components/color_picker/utils.js" :as eui]))

(def RGB_JOIN eui/RGB_JOIN)

(def parseColor eui/parseColor)

(def getFixedLinearGradient eui/getFixedLinearGradient)

(def HSV_FALLBACK eui/HSV_FALLBACK)

(def getChromaColor eui/getChromaColor)

(def getEventPosition eui/getEventPosition)

(def getLinearGradient eui/getLinearGradient)

(def RGB_FALLBACK eui/RGB_FALLBACK)

(def chromaValid eui/chromaValid)

(def HEX_FALLBACK eui/HEX_FALLBACK)
